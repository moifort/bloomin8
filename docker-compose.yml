services:
  canvas-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: canvas-server
    restart: unless-stopped
    environment:
      HOST: 0.0.0.0
      PORT: "3000"
      DATA_DIR: /data
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
